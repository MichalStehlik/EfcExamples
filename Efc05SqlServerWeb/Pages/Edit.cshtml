@page
@model Efc05SqlServerWeb.Pages.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Exhibit</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Exhibit.Id" />
            <div class="form-group">
                <label asp-for="Exhibit.Name" class="control-label"></label>
                <input asp-for="Exhibit.Name" class="form-control" />
                <span asp-validation-for="Exhibit.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Exhibit.Year" class="control-label"></label>
                <input asp-for="Exhibit.Year" class="form-control" />
                <span asp-validation-for="Exhibit.Year" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
